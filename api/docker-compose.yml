version: "3.7"
services:
  node:
    image: "node:latest"
    user: "node"
    working_dir: /home/node/app
    volumes:
      - ./:/home/node/app
    expose:
      - "4000"
    ports: # use if it is necessary to expose the container to the host machine
      - "4000:4000"
    command: "yarn start"
